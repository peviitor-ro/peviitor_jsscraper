const { Scraper } = require("./scraper");
const { Client } = require("./client");
const { generateJob, getParams } = require("./utils");

module.exports = {
  Scraper,
  Client,
  generateJob,
  getParams,
};
